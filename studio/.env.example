SANITY_STUDIO_PROJECT_ID=4qoowg94
SANITY_STUDIO_DATASET=production
# Server-side scripts only. Use an Editor token and never expose it to the app.
SANITY_AUTH_TOKEN=
PREFLIGHT_MINIMUM_APP_VERSION=0.1.0
# Required by export:bundle only when the generated checksum differs from the published release.
PREFLIGHT_CONTENT_BUNDLE_URL=
